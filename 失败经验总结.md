# 失败经验总结

## v2.0.3 (飞书自动同步)
- **文件替换异常**：使用了 `replace_file_content` 试图匹配复杂的代码块，但由于格式差异或工具限制导致匹配失败和代码覆盖。解决方法：使用 `read_file` 配合 `write_to_file` 重写整个文件部分，或利用 `cat` 获取准确内容。
- **Lint 误报**：临时文件引发了无关 Lint 错误，应忽略非生产代码的 Lint。
